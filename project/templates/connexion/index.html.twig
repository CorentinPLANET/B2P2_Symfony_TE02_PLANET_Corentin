{% extends 'base.html.twig' %}

{% block title %}Hello ConnexionController!
{% endblock %}

{% block body %}


	<div class="example-wrapper">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn">{{ button_label|default('Se Connecter') }}</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
